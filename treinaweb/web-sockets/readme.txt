#Precisamos instalar algumas bibliotecas.

#inserida no node_modules
npm install socket.io

#para subir o cliente é necessário um servidor de arquivos estáticos
#vamos instalar globalmente no CPU
#depois de instalado é dar o comando http-server para rodar o servidor
npm install http-server -g